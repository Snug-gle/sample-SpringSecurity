<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.iotree.sanghproject.mapper.UserMapper">

    <insert id="insertUser">
        INSERT INTO User (
            id, name, username, email, password, address, phone, website, company )
        VALUES (
            #{id}, #{name}, #{username}, #{email} ,#{password} ,#{address}, #{phone}, #{website}, #{company} )
    </insert>

    <update id="updateUser">
        UPDATE User
        SET id = #{id},
            name = #{name},
            username = #{username},
            email = #{email},
            password = #{password},
            address = #{address},
            phone = #{phone},
            website = #{website},
            company = #{company}
        WHERE id = ${id}
    </update>

    <delete id="deleteUser">
        DELETE
        FROM User
        WHERE id = #{id}
    </delete>

    <select id="getUserById" resultType="kr.co.iotree.sanghproject.vo.UserVo">
        SELECT *
        FROM User
        WHERE id = #{id}
    </select>

    <select id="getUserList" resultType="kr.co.iotree.sanghproject.vo.UserVo">
        SELECT *
        FROM User
    </select>
</mapper>